# good-pod.yaml (CORRECTED)
apiVersion: v1
kind: Pod
metadata:
  name: good-pod
spec:
  containers:
  - name: my-container
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    securityContext:      # Set the security context at the container level
      runAsNonRoot: true  # Enforce non-root execution
  securityContext:        # Set the security context at the pod level
    runAsNonRoot: true